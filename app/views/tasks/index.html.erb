<div>
  <h1>My Tasks</h1>
  <div>
    <% @tasks.each do |task| %>
    <p><input type="checkbox" <%= 'checked' if task.completed %>><%= link_to task[:title], task_path(task) %></p>
    <% end %>
    <% %>
    <% %>
  </div>
</div>
<footer><%= link_to "Create a task", tasks_new_path %></footer>
